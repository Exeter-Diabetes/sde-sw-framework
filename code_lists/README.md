This folder contains code lists data. The data is grouped together in categories such as conditions, sociodemographics/lifestyle, medications/devices, etc., and specific dataset a defined by:

* A set of code lists in CSV format.

* A markdown document called `info.md` which contain a brief description of the condition, the rules used to generate the code list datasets, and the original data sources used.