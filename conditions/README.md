This folder contains a list of subdirectories, one for each condition. Each subdirectory contains:

* A dataset of code lists in CSV format

* A markdown document called `description.md` which contain a brief description of the condition, the rules used to generate the code list datasets, links to scripts used to generate the code lists, and the original data sources used.